# Copy to .env, then edit. docker compose reads this automatically.
#   cp .env.example .env

# --- database ---------------------------------------------------------------
POSTGRES_PASSWORD=tankertrack_dev

# --- object storage (MinIO) -------------------------------------------------
S3_ACCESS_KEY_ID=tankertrack
S3_SECRET_ACCESS_KEY=tankertrack_dev_secret

# --- JWT secrets (REPLACE THESE) --------------------------------------------
# These are throwaway dev placeholders. Generate real ones:
#   node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
JWT_ACCESS_SECRET=dev_only_replace_me_0000000000000000000000000000
JWT_REFRESH_SECRET=dev_only_replace_me_1111111111111111111111111111
